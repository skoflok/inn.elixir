<h2> Админ-панель </h2>
<section class="row">
    <label for="chat-input" id="chat-label"></label>
</section>
<section class="row">
    <input id="chat-input" type="hidden" ></input>
</section>
<section class="row">
    <div id="messages" role="log" aria-live="polite">
    </div>
</section>
<section class="row">
    <div id="messages" role="log" aria-live="polite">
    </div>
</section>
<section class="row">
    <div id="table" role="archive" aria-live="polite">

        <%= for banned <- @list do %>
        <section class="row" id="banned-row-<%= banned.ip %>">
            <p><button type="button" onclick="tinApi.unbanned('<%= banned.ip %>')" class="button button-small"> Разбанить </button>
                [<%= banned.ip %>] <%= banned.time %>
            <p>
        </section>
        <% end %>
    </div>
</section>
<%= render(InnWeb.PageView, "paging.html", meta: @meta) %>