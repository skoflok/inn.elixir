<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Inn · Phoenix Framework</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body>
    <header>
      <section class="container-menu">
      <nav role="navigation">
        <ul>
          <li><%= link "Войти/Выйти", to: Routes.session_path(@conn, :new) %></li>
          <li><%= link "Главная", to: Routes.page_path(@conn, :index) %></li>
        <%= if @logged_user.is_logged_in do %>
          <li><%= @logged_user.user.email %></li>
          <li><%= link "Админ-панель", to: Routes.admin_path(@conn, :index) %></li>
          <li><%= link "Список забаненных", to: Routes.admin_path(@conn, :banned) %></li>
        <% end %>
      </nav>
      </section>
    </header>
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= @inner_content %>
    </main>
  </body>
</html>
